import type { SvgIconComponent } from "@mui/icons-material";

export type NavigationMap = Array<{ name: string, href: string }>;
export type SocialMap = Array<{ icon: SvgIconComponent, href: string }>;